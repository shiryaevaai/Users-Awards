@model IEnumerable<MvcApplication1.Models.Awards>

@if (Model.Count()==0)
{
    
    @:У данного пользователя пока нет наград
    
}
else
{
    <table class="table">
        <tr>
            <th>
                Title
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>      
        </tr>
    }

    </table>
}
